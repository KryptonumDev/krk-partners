import {{name}} from './{{name}}';
export type { Props as {{name}}Props } from './{{name}}.types';
export default {{name}};

export const {{name}}_Query = `
  _type == "{{name}}" => {
    _type,
  },
`;